# MOCK MODE: These env vars exist for future Supabase integration
# Currently, all services use mock implementations with setTimeout delays
# No actual API calls are made - these credentials are placeholders only
EXPO_PUBLIC_SUPABASE_URL=your-supabase-url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
